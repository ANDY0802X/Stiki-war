# STIKI WAR Arena

STIKI WAR is a 2D multiplayer stickman arena brawler built with React, Phaser, and Socket.IO.

## Run locally

```bash
npm run install:all
npm run dev
```

The client runs through Vite and the multiplayer server runs through Node.js.